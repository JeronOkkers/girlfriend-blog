/** next.config.ts */
const nextConfig = {
  // Make sure these properties are either absent or set as shown:
  // If you have a trailingSlash or basePath that’s wrong, comment it out for now.
  // trailingSlash: true,
  // basePath: '',
  // ...
};

export default nextConfig;
